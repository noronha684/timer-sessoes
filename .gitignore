# Código da API (vai no Worker timer-sessoes, deployado separadamente — não no Pages do frontend)
worker.js

# Configs locais do Claude Code / preview
.claude/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
